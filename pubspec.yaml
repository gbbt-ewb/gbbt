name: gbbt_bank
description: "GBBT Bank — a parody banking app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
    
  image_picker: ^1.1.2
  camera: ^0.11.0+2

  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1

  intl: ^0.20.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/journeys/