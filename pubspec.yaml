name: gbbt_bank
description: "GBBT Bank — a parody banking app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  audioplayers: ^6.0.0
    
  image_picker: ^1.1.2
  camera: ^0.12.0+2

  cupertino_icons: ^1.0.8
  google_fonts: ^8.2.1
  flutter_map: ^8.0.0
  latlong2: ^0.10.1
  intl: ^0.20.3
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/journeys/
    - assets/dating/
    - assets/audio1/